name: Run 1675 with Deviation ETF Tracker

on:
  schedule:
    # 日本時間 8:00〜16:00 の間に 1時間ごと実行（UTC 23〜7 時）
    - cron: '6 23 * * *'  # 8:00 JST
    - cron: '6 0 * * *'  # 9:00 JST
    - cron: '6 1 * * *'  # 10:00 JST
    - cron: '6 2 * * *'  # 11:00 JST
    - cron: '6 3 * * *'  # 12:00 JST
    - cron: '6 4 * * *'  # 13:00 JST
    - cron: '6 5 * * *'  # 14:00 JST
    - cron: '6 6 * * *'  # 15:00 JST
    - cron: '6 7 * * *'  # 16:00 JST
    - cron: '6 8 * * *'  # 17:00 JST
    - cron: '6 9 * * *'  # 18:00 JST
  workflow_dispatch:
  
permissions:
  contents: write

jobs:
  run-etf-tracker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance matplotlib pandas

      - name: Run 1675 ETF tracker
        run: |
          python 1675withDeviation.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main || true
          git add .
          git commit -m "Update 1675 ETF data" || exit 0
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
