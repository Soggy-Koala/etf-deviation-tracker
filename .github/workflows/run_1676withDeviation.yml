name: Run 1676 with Deviation ETF Tracker

on:
  schedule:
    # 日本時間 8:00〜16:00 の間に 1時間ごと実行（UTC 23〜7 時）
    - cron: '8 23 * * *'  # 8:00 JST
    - cron: '8 0 * * *'  # 9:00 JST
    - cron: '8 1 * * *'  # 10:00 JST
    - cron: '8 2 * * *'  # 11:00 JST
    - cron: '8 3 * * *'  # 12:00 JST
    - cron: '8 4 * * *'  # 13:00 JST
    - cron: '8 5 * * *'  # 14:00 JST
    - cron: '8 6 * * *'  # 15:00 JST
    - cron: '8 7 * * *'  # 16:00 JST
    - cron: '8 8 * * *'  # 17:00 JST
    - cron: '8 9 * * *'  # 18:00 JST
  workflow_dispatch:
  
permissions:
  contents: write

jobs:
  run-etf-tracker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance matplotlib pandas

      - name: Run 1676 ETF tracker
        run: |
          python 1676withDeviation.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main || true
          git add .
          git commit -m "Update 1676 ETF data" || exit 0
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
