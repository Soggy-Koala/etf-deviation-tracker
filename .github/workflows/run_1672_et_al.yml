name: run 1672_et_al
on:
  schedule:
    # 平日 (月〜金) JST で 8:00, 12:00, 16:00 に実行
    - cron: "0 23 * * 0-4"  # JST 8:00 (UTC 23:00 前日)
    - cron: "0 3 * * 0-4"   # JST 12:00 (UTC 3:00)
    - cron: "0 7 * * 0-4"   # JST 16:00 (UTC 7:00)
  workflow_dispatch:  # 手動実行も可

jobs:
  plot-etf:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 生成した画像をpushするために必要

    steps:
      - name: 📥 リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: 🐍 Python セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 依存関係インストール
        run: |
          python -m pip install --upgrade pip
          pip install pandas matplotlib

      - name: 📊 PNG 生成
        run: python plot_all_from_csv.py

      - name: 🧾 変更があればコミット
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add *.png
          if git diff --cached --quiet; then
            echo "No PNG updates — skipping commit."
          else
            git commit -m "Auto-update ETF charts"
            git push
          fi
